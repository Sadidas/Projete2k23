<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.projete.telasprojete">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/fox"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fox"
        android:supportsRtl="true"
        android:theme="@style/TelasProjete"
        tools:targetApi="33">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".InicialActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".NomeUserActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".ConfigActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".Sign1Activity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".SignActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".UserMainActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".MecanMainActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".ModeActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".EggActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".VeiculoActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".EstadoUserActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".MecanicoActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".NomeMecanicoActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

        <activity
            android:name=".MudarActivity"
            android:exported="true"
            android:theme="@style/TelasProjete">
        </activity>

    </application>
</manifest>